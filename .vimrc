" Config file storing personal preferences used for vim

" This is a WIP...Vim configurations are pretty much endless.

" Maintained by Michael Crawford <michael@crawford.io>

set nocompatible "this needs to be set before anything else

set shell=bash  " set which shell you're using
set showmode    " always show what mode you're on
set backspace=2 " make backspace work properly
set number      " always show line numbers
set ignorecase  " ignore case when searching
set autoread    " automatically reload files changed outside of vim

syntax on "enable syntax highlighting
